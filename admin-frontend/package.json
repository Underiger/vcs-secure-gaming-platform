{
  "name": "admin-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Virtual Casino Sandbox — 管理後台 Vue 3 SPA（部署於 /admin 路徑，獨立登入 + 2FA）",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "vue-tsc --noEmit",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "@casino/shared": "*",
    "axios": "^1.17.0",
    "pinia": "^3.0.1",
    "vue": "^3.4.31",
    "vue-router": "^4.4.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.5",
    "typescript": "^5.5.4",
    "vite": "^6.4.2",
    "vue-tsc": "^2.0.26"
  }
}
